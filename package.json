{
  "name": "stack",
  "version": "0.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "start": "npm run build && node .",
    "clean": "rm -rf dist && rm -rf app",
    "build": "npm run clean && tsc && babel app --out-dir dist --copy-files",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/winston": "^2.3.9",
    "express": "^4.16.3",
    "typings": "^2.1.1",
    "winston": "^3.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.16.0",
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-register": "^6.26.0",
    "tslint": "^5.10.0",
    "typescript": "^2.9.1"
  }
}
