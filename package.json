{
  "name": "stack",
  "version": "0.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "start": "NODE_ENV=production npm run build && node .",
    "dev": "NODE_ENV=development npm run build && node .",
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/winston": "^2.3.9",
    "tslint": "^5.10.0",
    "express": "^4.16.3",
    "typings": "^2.1.1",
    "winston": "^3.0.0",
    "typescript": "^2.9.1"
  }
}
