{
  "name": "stack",
  "version": "0.0.0",
  "description": "",
  "main": "dist/main.js",
  "scripts": {
    "start": "export NODE_ENV=production && npm run build && pm2 start ./dist/main.js",
    "dev": "export NODE_ENV=development && npm run build && node .",
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "push": "git add . && git commit -m \"Saving Progress\" && git push",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.16.0",
    "@types/winston": "^2.3.9",
    "express": "^4.16.3",
    "ts-node": "^7.0.1",
    "tslint": "^5.10.0",
    "typescript": "^2.9.1",
    "typings": "^2.1.1",
    "winston": "^3.0.0"
  }
}
